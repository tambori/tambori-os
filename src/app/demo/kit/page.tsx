

import { Main, Section, Container } from "@/components/craft";

export default function KitPage() {

  return (
    <Main className="!p-0 h-screen">
      <Section>
        <Container  className="mx-auto grid sm:max-w-[30rem] max-w-full flex-1 auto-rows-max lg:gap-12 gap-4">
          <h1 className="text-balance text-lg font-normal text-center">The BlogKit demo will soon be available, kindly check back later.</h1>
        </Container>
      </Section>
    </Main>
  )
}
